package com.example.demo.entity;
//This Enum and types in it defines strict categories for placing
// a status of an order
public enum OrderStatus {
    NEW,
    ACCEPTED,
    PREPARING,
    READY,
    COLLECTED,
    CANCELLED
}
